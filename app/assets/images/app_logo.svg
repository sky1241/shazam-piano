<svg viewBox="0 0 108 108" xmlns="http://www.w3.org/2000/svg">

  <!-- Dégradés -->
  <defs>
    <linearGradient id="bgGrad" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#0B0F10"/>
      <stop offset="100%" stop-color="#0B0F10"/>
    </linearGradient>
    <linearGradient id="mainGrad" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#2AE6BE"/>
      <stop offset="100%" stop-color="#21C7A3"/>
    </linearGradient>
  </defs>

  <!-- Fond carré arrondi -->
  <rect x="6" y="6" width="96" height="96" rx="24" fill="url(#bgGrad)"/>

  <!-- Bloc de touches de piano -->
  <rect x="22" y="60" width="64" height="26" rx="6" fill="#0B0F10"/>

  <!-- 5 touches de piano -->
  <rect x="24" y="60" width="10" height="24" rx="3" fill="#e5e7eb"/>
  <rect x="36" y="60" width="10" height="24" rx="3" fill="#f3f4f6"/>
  <rect x="48" y="60" width="10" height="24" rx="3" fill="#f9fafb"/>
  <rect x="60" y="60" width="10" height="24" rx="3" fill="#f3f4f6"/>
  <rect x="72" y="60" width="10" height="24" rx="3" fill="#e5e7eb"/>

  <!-- 3 traits "notes à jouer" -->
  <!-- Touche 3 : note à venir (ne touche pas le clavier) -->
  <rect x="53" y="18" width="2" height="20" rx="1" fill="url(#mainGrad)"/>

  <!-- Touche 4 : note jouée (descend presque jusqu'au clavier) -->
  <rect x="65" y="27" width="2" height="31" rx="1" fill="url(#mainGrad)"/>

  <!-- Touche 5 : note à venir (même top que la 1, ne touche pas le clavier) -->
  <rect x="77" y="18" width="2" height="26" rx="1" fill="url(#mainGrad)"/>

  <!-- Clé de sol à gauche -->
  <text x="34" y="38"
        text-anchor="middle"
        dominant-baseline="middle"
        font-size="40"
        fill="url(#mainGrad)"
        font-family="serif">
    𝄞
  </text>

</svg>

